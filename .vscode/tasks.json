{
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "make && make run",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}