package core.func;

public interface StoreFunction {

    /**
     * A function that when evaluated will return the data value to be stored into
     * memory
     * 
     * @return The address
     */
    public char evaluate();
}
