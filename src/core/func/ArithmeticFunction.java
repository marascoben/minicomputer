package core.func;

public interface ArithmeticFunction {

    /**
     * Evaluates the supplied arithmetic function on the given input, and returns
     * the resultant
     * 
     * @param value The value to evaluate
     * @return The result
     */
    public char evaluate(char value);
}
